<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"      
	"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd"> 
	
<mapper namespace="net.kingkid.SalesPromote.mapper.UserMapper">


	<insert id="addnew"
		parameterType="net.kingkid.SalesPromote.entity.User"
		useGeneratedKeys="true"
		keyProperty="id">
		INSERT INTO user (
			id,username, password
		) VALUES (
			#{id},
			#{username}, #{password}
		)
	</insert>
	

	<delete id="deleteUserById" parameterType="java.util.List">
        DELETE FROM user 
		WHERE 
		    user.id
		IN
        <foreach item="id" collection="ids" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
	<update id="updateInfo">
		UPDATE 
			user
		SET
			password=#{password},
			username=#{username}
		WHERE
			id=#{id}
	</update>
	

	
	
	
	<select id="findByUsername"
		resultType="net.kingkid.SalesPromote.entity.User">
		SELECT 
			id, username, password
		FROM 
			user
		WHERE 
			username=#{username}
	</select>

	<select id="findAll"
		resultType="net.kingkid.SalesPromote.entity.User">
		SELECT 
			id, username, password
		FROM 
			user
		
	</select>

	<select id="findById"
		resultType="net.kingkid.SalesPromote.entity.User">
		SELECT 
			id,
			username,
			password
		FROM 
			user
		WHERE 
			id=#{id}
	</select>

</mapper>





